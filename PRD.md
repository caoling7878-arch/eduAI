# AI 智慧教育云平台 — 产品需求文档（PRD）

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.5 |
| 状态 | Draft |
| 目标读者 | 产品 / 前端 / 后端 / AI / 运维 |
| 更新日期 | 2026-07-31 |
| 产品代号 | eduAI |
| 变更摘要 | v1.5 取消 Docker；P0 全量分步高质量交付（本地进程部署） |

---

## 1. 文档说明

### 1.1 编写目的

将「AI 智慧教育云平台」从营销描述解码为可评审、可排期、可验收的产品与工程规格，明确：

- 真实要解决什么问题（而非口号）
- MVP 与后续版本边界
- 前后端最可行技术路径
- 学员端 / 管理端信息架构与视觉方向（参考截图，**不照搬**）

### 1.2 信息来源与可信度分层

| 层级 | 来源 | 处理方式 |
|------|------|----------|
| L1 已验证界面 | 管理后台仪表盘、学员首页截图 | 作为功能范围与模块清单的主依据 |
| L1b 已接入开源能力 | [wy51ai/edulab](https://github.com/wy51ai/edulab)（Apache-2.0） | 立体几何 / 解析几何交互课页生成；本地 vendoring 于 `vendor/edulab` |
| L2 商业叙事 | 数据治理 / 模型训练 / 样本回流等宣传 | 拆解为能力目标，按分期落地，避免一次性过度建设 |
| L3 变现模式 | 私有化 / SaaS / 定制 / 资源售卖 | 作为商业约束影响权限、计费、多租户设计 |

> **产品判断**：截图呈现的是「内容 + 教学运营 + AI 助手/知识库/课件生成」型教育 SaaS；宣传中的「自建分布式训练 / 灰度推理平台」属于高成本能力，**不应作为首期必交付项**，而以「接入大模型 API + RAG + 可观测批改流水线」替代，在商业上同样能讲清「AI 闭环」。

### 1.3 成功标准（商业与产品）

1. **本地进程可私有化部署**：**不使用 Docker**；Python API + Vite 前后端本地启动即可演示。
2. **教学闭环可演示**：建课 → 组班 → 学习/练习 → 批改/学情 → AI 辅导，全链路可走通。
3. **AI 可替换**：模型供应商可配置（OpenAI 兼容协议），不绑死单一云厂商。
4. **可售**：支持基础免费 + 会员/付费课 + 机构私有化授权。
5. **几何动图可演示**：基于 edulab 一键生成立体几何 / 解析几何交互课页，并可挂入课时学习。

---

## 2. 问题与机会

### 2.1 行业痛点（解码后）

| # | 宣传表述 | 产品化问题 | 首期解法 |
|---|----------|------------|----------|
| 1 | 教学数据分散、无版本 | 题库/课件/知识库缺少统一资源中心与版本 | 资源中心 + 版本号 + 导入导出 |
| 2 | 批改标准不统一 | 客观题规则批改；主观题需 rubric + 人工复核 | 规则引擎 + LLM 初评 + 教师复核队列 |
| 3 | 训练上线监控割裂 | 中小客户极少自训模型 | **首期不做训练平台**；预留「样本回流」与「Prompt/模型配置」 |
| 4 | 无审计日志 | 校园/政企合规刚需 | 操作审计 + 登录安全 + RBAC |
| 5 | 无样本回流 | 错题/批改异议无法沉淀 | 错题本 + 标注队列回流数据集（P1/P2） |

### 2.2 目标用户与场景

| 角色 | 核心诉求 | 主入口 |
|------|----------|--------|
| 学员 / 学员家长 | 学课、练习、打卡、AI 答疑、看公告 | 学员端 |
| 教师 | 建课组班、出卷批改、学情、AI 出题/课件、几何动图课页 | 教师工作台（可复用管理端子集） |
| 数学/理科教师 | 立体几何与圆锥曲线可视化讲解、随机变式题 | 几何动图实验室 |
| 机构管理员 | 用户/权限、订单会员、内容运营、数据看板 | 管理后台 |
| 平台超管 | 多租户、计费、模型 Key、系统配置 | 超管控制台 |
| 实施/二开方 | 部署、对接 LMS、定制模块 | API + 部署文档 |

### 2.3 非目标（首期明确不做）

- 自建 GPU 分布式训练调度平台
- 完整 GraphQL 联邦（首期 REST + OpenAPI 即可）
- 直播课堂 / RTC（可作为后续独立模块）
- 原生 App（首期 PWA / 响应式 Web）
- 高度定制的本地小模型微调流水线（P2+ 可选）

---

## 3. 产品定位与价值主张

**一句话**：面向学校、教培与企业内训的 **AI 原生智慧教学云**，用「内容生产 × 学习行为 × 评估反馈」闭环提升教学与内容生产效率，支持私有化与 SaaS 双模式变现。

**差异化（务实版）**：

1. AI 能力内嵌业务（助手、知识库、出题、课件、批改），而非仅挂直播。
2. 资源与学情数据可版本、可导出、可审计。
3. 部署成本低，API 可对接第三方 LMS。
4. **几何动图教学**：接入 edulab，用 sympy 精确内核生成可交互 3D/2D 课页，「算对」与「看懂」同源，区别于纯录播或静态 PPT。

---

## 4. 产品信息架构

### 4.1 双端结构

```
eduAI
├── 学员端（Learning Portal）     # 学习、练习、AI 辅导、个人中心、几何动图课页
├── 管理后台（Admin Console）     # 运营、内容、AI 配置、数据、权限、几何实验室
├── 教师工作台（Teacher Hub）     # 首期可用管理端角色视图；P1 独立简化入口
├── 几何动图实验室（Geometry Lab）# edulab：交互课页生成与预览（可嵌入课时）
└── 开放 API（Public API）        # LMS / 第三方集成
```

### 4.2 管理后台模块（对齐截图并补齐 AI 闭环）

| 模块 | 优先级 | 说明 |
|------|--------|------|
| 仪表盘 | P0 | KPI、趋势、排行、待办 |
| 课程管理 | P0 | 课程、章节、课时、上下架、价格/会员属性 |
| 班级管理 | P0 | 班级、学员归属、教师绑定 |
| 用户管理 | P0 | 学员账号、状态、标签 |
| 教师管理 | P0 | 教师档案、授课权限 |
| 电子书管理 | P1 | 数字读物 / 章节阅读进度 |
| 题库管理 | P0 | 题目 CRUD、知识点标签、导入导出、版本 |
| 试卷管理 | P0 | 组卷、发布、作答统计 |
| 试卷模板 | P1 | 可复用试卷结构 |
| PPT 模板 | P1 | 课件版式资产 |
| PPT 生成助手 | P0 | 大纲 → 幻灯片结构化生成 |
| 几何动图实验室 | P0 | 基于 edulab 生成/预览/挂课：立体几何、解析几何（化学微观为 P1 扩展） |
| AI 助手管理 | P0 | 角色人设、模型、温度、知识库绑定 |
| 知识库管理 | P0 | 文档入库、切片、向量化、检索测试 |
| 工作流管理 | P2 | 自动化编排（批改流、通知流）；首期可用固定流水线 |
| 公告管理 | P0 | 全站/定向公告 |
| 订单与会员 | P0 | 付费课、会员套餐、订单流水 |
| 反馈与工单 | P1 | 待处理反馈（仪表盘已暴露） |
| 学情与报表 | P1 | 班级/个人薄弱点、导出 |
| 智能批改中心 | P1 | 批改队列、复核、抽样质检 |
| 样本与数据集 | P2 | 错题/异议回流、导出训练样本 |
| 模型与 Prompt 配置 | P1 | 供应商、Key、路由、Prompt 版本（替代「训练平台」MVP） |
| 权限与审计 | P0 | RBAC、操作日志、登录日志 |
| 系统设置 | P0 | 站点、存储、短信/邮件、主题 |
| 返回前台 | P0 | 跳转学员端 |

### 4.3 学员端模块（对齐截图并美化重构）

| 模块 | 优先级 | 说明 |
|------|--------|------|
| 首页发现 | P0 | 热门课程、推荐、每日内容 |
| AI 学伴列表 / 对话 | P0 | 多角色助手、流式对话、引用知识库 |
| 课程学习 | P0 | 目录、视频/图文、**交互几何课页**、进度 |
| 几何动图学习 | P0 | 浏览实验室画廊、打开 3D/2D 交互课页、记录学习时长 |
| 打卡 | P0 | 连续打卡、激励 |
| 今日计划 | P0 | 学习任务 CRUD、完成状态 |
| 最新公告 | P0 | 列表与详情 |
| 每日单词 / 微学习 | P1 | 单词卡、进度、设置 |
| 每日美文 / 阅读 | P1 | 阅读内容流 |
| 小学数学计算专项 | P3 | 1–6 年级计算题库、每日练习、错题本 |
| 练习与考试 | P0 | 答题、交卷、结果 |
| 错题本 | P1 | 错题复习、回流标注 |
| 个人中心 | P0 | 资料、会员、订单、学习时长；P3 起含数据导出/注销 |
| 消息中心 | P1 | 系统通知 |

---

## 5. 视觉与交互设计方向（美化，不照搬）

### 5.1 对截图的诊断

**管理端问题**：KPI 卡片彩虹渐变过强、图表配色杂乱、信息密度高但品牌感弱。  
**学员端问题**：典型「紫主色 + 三栏卡片墙」，同质化严重；右侧小部件堆叠，首屏叙事分散。

### 5.2 设计原则（必须遵守）

1. **一屏一叙事**：学员首页首屏 = 品牌 + 一句价值主张/今日目标 + 主 CTA + 一个主视觉，不做仪表盘式堆砌。
2. **品牌优先**：产品名在首屏具备英雄级存在感，去掉导航后仍能识别品牌。
3. **避免 AI 套版审美**：不用紫白渐变套版、奶油风衬线+陶土色、报纸风细线密栏；也不默认暗黑+霓虹光晕。
4. **真实视觉锚点**：课程封面、教室/实验/阅读场景摄影或插画，抽象渐变不得充当主视觉。
5. **卡片克制**：默认少卡片；卡片仅用于可交互单元（课程、助手会话入口）。KPI 用克制色块+数据字重，而非高饱和渐变砖。
6. **动效有目的**：至少 2–3 处（页面入场、助手在线呼吸、课程 hover 微动），禁止装饰噪音。

### 5.3 建议品牌视觉系统

| Token | 建议值 | 用途 |
|-------|--------|------|
| `--brand` | `#0F6B5C`（深青松） | 主色、CTA、关键态 |
| `--brand-accent` | `#E8A317`（暖琥珀） | 强调、打卡、成就 |
| `--ink` | `#14212B` | 标题 |
| `--muted` | `#5C6B73` | 次要文案 |
| `--surface` | `#F3F6F4` → `#E7EEF5` 轻渐变底 | 页面氛围（非纯平） |
| `--success` | `#2F8F6B` | 在线、完成 |
| 字体 | **「思源宋体 / Source Han Serif」标题 +「IBM Plex Sans / 思源黑体」正文** | 教育气质，避开 Inter/系统默认堆 |

> 主题可在系统设置切换「青松教育风 / 深空教研风」，但默认浅色专业风。

### 5.4 学员端布局重构（相对截图）

```
┌─────────────────────────────────────────────────────────────┐
│ Top Nav：Logo(品牌) · 搜索 · 学习中心 · 题库 · 会员 · 头像   │
├──────────────────┬──────────────────────────┬───────────────┤
│ AI 学伴（窄栏）   │ 主舞台                   │ 今日（可折叠） │
│ 列表 + 在线态     │ Hero：今日学习目标+主CTA │ 打卡条（非大红卡）│
│ 点击进入全屏对话  │ 课程横向叙事带（非密网格）│ 计划清单        │
│                   │ 下方：阅读/美文一条流     │ 公告精简 3 条   │
│                   │                          │ 微学习（单词）  │
└──────────────────┴──────────────────────────┴───────────────┘
```

**相对截图的关键变化**：

- 缩小右侧噪音，默认折叠「公告/单词」为次级 Tab。
- 课程区改「横向精选带 + 查看全部」，避免首屏 6 张卡墙。
- AI 学伴保留，但进入对话用抽屉/全页，避免侧栏永久占宽。
- 「立即打卡」用琥珀强调条，替代大红渐变块。

### 5.5 管理端布局重构（相对截图）

- 侧栏：浅色或低对比深色，分组（教学运营 / 内容资源 / AI 中心 / 系统）。
- KPI：统一品牌色透明度层级，最多 1 个强调色；趋势用同色系多色阶，不用彩虹柱。
- 图表：ECharts 主题统一；空状态给引导 CTA（「去发布课程」）。
- 仪表盘分区：概览 → 学情 → 经营 → AI 健康度（Token 消耗、批改队列积压）。

---

## 6. 核心用户故事与验收标准

### 6.1 P0 用户故事

#### US-01 机构管理员查看经营与教学概览
**作为**管理员，**我希望**在仪表盘看到用户、课程、活跃、订单、收入、反馈、学习时长，**以便**快速决策。  
**验收**：

- 展示不少于 8 个 KPI（可配置显隐）
- 近 30 日：活跃趋势、用户增长、收入趋势、学习行为分布
- 活跃用户 / 活跃班级 Top N
- 接口耗时 P95 < 500ms（聚合可走日表）

#### US-02 配置 AI 学伴并在学员端对话
**作为**管理员，**我希望**创建带人设与知识库的 AI 助手；**作为**学员，**我希望**流式对话并看到引用片段。  
**验收**：

- 助手：名称、头像、系统 Prompt、模型、温度、绑定知识库、启用状态
- 学员端列表展示在线态（基于服务健康，非伪随机）
- SSE/WebSocket 流式输出；支持停止生成
- 知识库命中时展示引用（文档名 + 片段）

#### US-03 知识库入库与检索
**作为**教师/管理员，**我希望**上传 PDF/Markdown/DOCX，自动切片向量化，并试检索。  
**验收**：

- 支持 PDF、MD、TXT、DOCX
- 显示解析状态：排队 / 处理中 / 成功 / 失败
- 提供检索调试台（query → topK chunks）

#### US-04 课程售卖与学习
**作为**学员，**我希望**浏览公开/会员/付费课并学习；权限不正确时引导开通。  
**验收**：

- 课程属性：公开 / 会员 / 付费
- 未授权拦截 + 引导
- 学习进度可续看；累计学习时长入统计

#### US-05 组班与成员
**作为**教师，**我希望**创建班级、批量导入学员、绑定课程。  
**验收**：Excel/CSV 导入；失败行可下载错误报告。

#### US-06 题库与客观题考试
**作为**教师，**我希望**维护题库并组卷；学员作答后自动出分。  
**验收**：

- 题型：单选、多选、判断、填空（精确/归一化匹配）
- 交卷即时得分；错题写入错题本（可延后到 P1，但表结构 P0 预留）

#### US-07 PPT 生成助手
**作为**教师，**我希望**输入主题/大纲生成可编辑幻灯片结构并导出。  
**验收**：

- 输出页标题、要点、讲者备注
- 可套模板；导出 PPTX 或 JSON 中间格式
- 全流程可追踪 Token/耗时

#### US-08 权限与审计
**作为**超管，**我希望**按角色授权并查询操作日志。  
**验收**：

- 角色：超管 / 机构管理员 / 教师 / 学员（可扩展）
- 关键写操作记审计：谁、何时、资源、前后摘要、IP
- 日志只追加、管理端可检索导出

#### US-09 公告、打卡、今日计划
**验收**：公告 CRUD + 学员可见；打卡每日一次与连续天数；计划增删改完成。

#### US-10 几何动图交互课页（edulab）
**作为**数学教师，**我希望**一键生成立体几何 / 解析几何交互课页并挂到课时；**作为**学员，**我希望**旋转模型或拖动参数滑块配合分步讲解理解题意。  
**验收**：

- 管理端/CLI 可生成：正方体线面角、长方体体积、棱锥线面角、随机立体几何；解析几何已注册题型（椭圆数量积范围等）
- 课页为自包含 HTML，可静态托管或经 API 异步生成后回写 URL
- 学员端可从「几何动图实验室」画廊或课时内打开；学习时长计入行为统计
- 答案卡数值与交互控件读数同源（sympy kernel），不得出现「讲错几何」
- 许可证合规：保留 Apache-2.0 NOTICE，产品致谢标明上游 edulab

### 6.2 P1 关键故事（摘要）

- 主观题 LLM 初评 + 教师复核
- 电子书与阅读进度
- 每日单词 / 美文运营位
- 学情报表导出（班级薄弱知识点）
- 反馈工单处理
- Prompt/模型路由与成本看板
- 试卷模板、PPT 模板库
- 几何实验室：文字/图片题目入口（视觉读题）+ 化学反应微观动图（edu-chem-reaction）
- 几何课页与知识点/题库题目双向关联

### 6.3 P2 关键故事（摘要）

- 工作流可视化编排
- 样本回流数据集与导出（对接外部微调）
- 学习路径自动推荐引擎
- LMS LTI/REST 深度对接
- 多租户计费与用量包

### 6.4 P3 / 扩展课程用户故事（摘要）

- **小学数学计算专项**：选年级 → 设定每日题量（10–100）→ 每日一页随机练习 → 提交判分 → 错题订正并入错题本；分数竖式显示；计时与练习记录
- **PPT 导出**：管理端生成大纲后下载可编辑 `.pptx`
- **合规**：学员导出个人数据 JSON；确认后注销账号；运维定期 `tools/backup.sh`

---

## 7. 八大能力板块 → 工程落地图

| 宣传板块 | 工程映射 | 分期 |
|----------|----------|------|
| 1. 数据治理 | `resource` 服务：题库/数据集/导入导出/版本 | P0 基础，P1 完善 |
| 2. AI 标注与智能批改 | OCR（可选）+ 规则批改 + LLM 主观评 + 标注队列 | P0 客观；P1 主观+标注 |
| 3. 全链路模型管理 | **降维**：Model Provider / Prompt 版本 / 推理网关监控；训练平台不做 | P1 |
| 4. 个性化智能教学 | 出题、讲义、PPT、助手、薄弱点练习推送、**几何动图课页** | P0 生成+几何；P1 路径 |
| 5. 样本回流闭环 | 错题/复核差异 → dataset 样本 | P2 |
| 6. 可视化仪表盘 | 管理端 Dashboard + 报表导出 | P0 |
| 7. 权限与审计 | RBAC + Audit Log | P0 |
| 8. API 与部署 | OpenAPI、Webhook、本地进程部署（无 Docker） | P0 |

---

## 8. 功能需求详述（按域）

### 8.1 账号与权限域

**功能**：注册/登录（邮箱或手机）、JWT 访问令牌 + Refresh、RBAC、机构（租户）隔离。

**角色权限矩阵（首期）**：

| 能力 | 超管 | 机构管理员 | 教师 | 学员 |
|------|------|------------|------|------|
| 系统配置 / 模型 Key | ✓ | 只读或禁止 | ✗ | ✗ |
| 用户/教师/班级 | ✓ | ✓ | 本班 | 只读自己 |
| 课程/题库/试卷 | ✓ | ✓ | ✓（授权范围） | 学习/作答 |
| AI 助手/知识库 | ✓ | ✓ | ✓ | 使用 |
| 订单退款 | ✓ | ✓ | ✗ | 自己的订单 |
| 审计日志 | ✓ | ✓ | ✗ | ✗ |

**多租户**：`tenant_id` 行级隔离；私有化单租户部署时租户=默认。

### 8.2 课程与内容域

- 课程：标题、封面、简介、标签、价格类型、上下架、排序、学习人数冗余字段
- 章节树：章 → 课时（视频 URL / 富文本 / 文档 / **`interactive_lab` 几何动图课页 URL**）
- 权限校验中间件：`public | member | paid | class_only`
- 学习进度：`user_id + lesson_id + progress_pct + last_position`

### 8.3 班级与教学域

- 班级绑定课程、班主任、学员列表
- 班级公告（可选）
- 学情聚合按 `class_id`

### 8.4 题库 / 试卷 / 批改域

**题目模型关键字段**：

- `type`, `stem`, `options`, `answer`, `analysis`, `knowledge_points[]`, `difficulty`, `version`, `media[]`

**批改策略**：

| 题型 | 策略 |
|------|------|
| 客观题 | 服务端规则引擎，不可信任前端得分 |
| 填空 | 归一化（全半角、大小写、别名表） |
| 主观题 | LLM 按评分标准打分 + 理由；状态 `ai_scored / teacher_reviewed` |
| 拍照题（可选） | OCR → 文本 → 同上 |

**试卷**：固定卷 / 随机抽题（按知识点与难度配额）。

### 8.5 AI 中心域

#### 8.5.1 助手（Agents）

- 配置：persona、tools 开关、知识库 IDs、限流、可见范围（全站/课程/班级）
- 会话：`thread` + `messages`；支持重新生成、点赞点踩（回流信号）

#### 8.5.2 知识库（RAG）

流水线：上传 → 解析 → 分块 → Embedding → 向量库 → 检索 → 拼 Prompt。

参数可配：`chunk_size`, `overlap`, `top_k`, `score_threshold`。

#### 8.5.3 生成能力

- 试题生成：输入知识点/难度/题型数量 → 结构化 JSON → 人工确认入库
- 讲义生成：Markdown
- PPT 生成：幻灯片 JSON → PPTX

所有生成任务进 `generation_jobs` 表，支持异步与失败重试。

#### 8.5.4 推理网关

统一 `llm_gateway`：

- 供应商适配（OpenAI Compatible）
- Key 管理与租户配额
- 超时、重试、熔断
- Token/费用记账

### 8.6 运营与增长域

- 公告、打卡、每日单词、每日美文、反馈
- 会员：套餐、权益（AI 次数、会员课）
- 订单：创建、支付回调（微信/支付宝可插拔；演示可用「模拟支付」）

### 8.7 数据与仪表盘域

**日聚合表**（避免实时扫流水）：

- `agg_daily_active_users`
- `agg_daily_revenue`
- `agg_daily_learning_duration`
- `agg_daily_activity_breakdown`

定时任务每日/每小时滚动。

### 8.8 审计与安全域

- 审计日志、登录失败锁定、敏感字段脱敏、文件类型白名单与病毒扫描钩子（可插拔）
- 管理端所有删除需二次确认；高危操作可强制理由

---

## 9. 技术方案（最可行路径）

### 9.1 选型原则

1. 一个人也能部署演示；小团队能持续迭代。  
2. AI 以 **API + RAG** 为主，不自建训练平台。  
3. 前后端类型共享，减少对接成本。  
4. 本地进程部署优先；K8s 为可选后续。

### 9.2 推荐总体架构

```
                    ┌──────────────┐
                    │  Nginx / Caddy│
                    └──────┬───────┘
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌────────────┐  ┌────────────┐  ┌────────────┐
    │ 学员端 Web │  │ 管理端 Web │  │  OpenAPI   │
    │ (Vue/React)│  │ (同构)     │  │  Gateway   │
    └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
          └───────────────┼───────────────┘
                          ▼
                 ┌─────────────────┐
                 │  API Server     │  NestJS 或 FastAPI
                 │  (REST + SSE)   │
                 └────────┬────────┘
          ┌───────────────┼────────────────┐
          ▼               ▼                ▼
   ┌────────────┐  ┌────────────┐  ┌──────────────┐
   │ PostgreSQL │  │   Redis    │  │ 对象存储 MinIO│
   │ + pgvector │  │ 缓存/队列  │  │ 课件/文档     │
   └────────────┘  └─────┬──────┘  └──────────────┘
                         ▼
                  ┌─────────────┐     ┌─────────────┐
                  │ Worker 异步  │────▶│ LLM Provider│
                  │ 解析/批改/生成│     │ Embedding   │
                  └─────────────┘     └─────────────┘
```

### 9.3 技术栈推荐（默认方案 A：高性价比）

| 层 | 选型 | 理由 |
|----|------|------|
| 学员端 + 管理端 | **Vue 3 + Vite + TypeScript** | 与截图技术痕迹（Vite:5173）一致；生态成熟；中文资料多 |
| UI | 学员端 **自研设计系统（UnoCSS/Tailwind）**；管理端 **Naive UI 或 Element Plus** 深度主题定制 | 学员端摆脱后台组件感；管理端提效 |
| 图表 | ECharts | 仪表盘足够 |
| 后端 | **FastAPI（Python）** | AI/OCR/文档解析生态最好，RAG 落地快 |
| ORM | SQLAlchemy 2 / SQLModel | — |
| DB | **PostgreSQL 16 + pgvector** | 一套库解决业务+向量，运维最简 |
| 缓存/队列 | Redis + Arq 或 Celery | 解析与生成异步化 |
| 文件 | MinIO（S3 API） | 私有化友好 |
| 实时 | SSE（对话流优先） | 比 WS 更简单，够用 |
| 认证 | JWT + Refresh；密码 Argon2 | — |
| 文档解析 | Unstructured / PyMuPDF + 自研分块 | — |
| PPTX 导出 | python-pptx | — |
| 部署 | 本地进程（uvicorn + Vite）；不用 Docker | — |
| 可观测 | OpenTelemetry + Loki/Prometheus（P1） | — |

**备选方案 B（Node 统一语言）**：NestJS + Prisma + BullMQ；AI 调用仍走 HTTP。适合前端团队无 Python 经验时选择。AI 文档解析质量通常略逊于 Python 栈，可用独立 `ai-worker` 补齐。

### 9.4 前端工程结构（建议 Monorepo）

```
apps/
  web/                 # 学员端
  admin/               # 管理端
packages/
  ui/                  # 设计系统与品牌组件
  shared/              # 类型、工具、API SDK
  config/              # ESLint、TSConfig
```

工具：pnpm workspace + Turborepo（可选）。

### 9.5 后端服务边界（模块化单体，禁止首期微服务拆炸）

```
apps/api/
  modules/
    identity/      # 用户、角色、租户、认证
    catalog/       # 课程、电子书
    classroom/     # 班级、计划、打卡
    assessment/    # 题库、试卷、批改
    ai/            # 助手、知识库、生成、网关
    commerce/      # 会员、订单
    ops/           # 公告、反馈
    analytics/     # 仪表盘聚合
    audit/         # 审计
    system/        # 配置、文件
    geometry_lab/  # edulab 封装：生成任务、课页元数据、挂课
```

> **模块化单体 + 异步 Worker** 是本项目最可行形态；待 DAU 与团队规模上来再拆 `ai-service`。  
> 几何课页生成调用 `tools/geometry-lab/generate.py`（包装 `vendor/edulab`），重计算放 Worker，勿阻塞 API。

### 9.6 核心数据模型（精简 ER）

```
tenants ─┬─ users ── user_roles ── roles ── permissions
         ├─ courses ── chapters ── lessons
         ├─ classes ── class_members
         ├─ questions ── question_versions
         ├─ papers ── paper_questions ── submissions ── answers
         ├─ ai_assistants ── kb_bindings
         ├─ knowledge_bases ── documents ── chunks
         ├─ conversations ── messages
         ├─ orders ── memberships
         ├─ announcements / checkins / study_plans / daily_words
         ├─ lab_pages ── lab_generation_jobs   # 几何动图课页与生成任务
         └─ audit_logs / llm_usage_logs / generation_jobs
```

### 9.7 API 约定

- 风格：REST，`/api/v1/...`
- 错误：`{ code, message, details }`
- 分页：`page, page_size, total`
- 对话流：`POST /ai/chat/stream` → SSE
- 开放能力：P1 提供 API Token + 范围权限；GraphQL **不做首期**

### 9.8 AI 成本与安全护栏（必须写进实现）

1. 租户级日配额、学员级频率限制  
2. Prompt 注入基础防护（系统指令隔离、知识库片段转义）  
3. 内容安全审核钩子（可接第三方）  
4. 全部 LLM 调用落 `llm_usage_logs`  
5. 教师生成内容默认「待确认」再入库题库  

### 9.9 部署方式（明确：不用 Docker）

**取消 Docker / Docker Compose。** 私有化以本地/系统进程方式运行：

| 进程 | 命令 | 端口 |
|------|------|------|
| API | `python3 -m uvicorn app.main:app --reload --port 8000 --app-dir apps/api` | 8000 |
| 学员端 | `npm run dev --prefix apps/web` | 5173 |
| 管理端 | `npm run dev --prefix apps/admin` | 5174 |
| 几何课页生成 | `python3 tools/geometry-lab/generate.py demos` | CLI |

数据：默认 **SQLite**（`apps/api/data/eduai.db`）；文件落本地 `apps/api/data/uploads/`。  
环境变量见 `apps/api/.env.example`、`apps/web/.env.example`。

---

## 10. 分期路线图

### Phase 0 — P0 全量分步交付（进行中）

> **决策**：取消 Docker。除 Docker 外，PRD §4.2 / §4.3 全部 P0 模块按下列步骤高质量完成。  
> **已完成地基**：热门课学员端、账号进度、AI 课堂、几何/英语/编程、主页。

| 步骤 | 范围 | 状态 |
|------|------|------|
| S0 | 热门课 + 账号进度 + AI 课堂 + 主页 | ✅ |
| S1 | 后端域模型扩展 + RBAC（admin/teacher/student）+ 审计 + 种子数据 | ✅ |
| S2 | 管理端：仪表盘 / 用户 / 教师 / 课程 / 班级 | ✅ |
| S3 | 管理端：题库 / 试卷 / 公告 / 订单会员 / 系统设置 | ✅ |
| S4 | 管理端：AI 助手 / 知识库 / PPT 生成 / 几何实验室入口 | ✅ |
| S5 | 学员端：打卡 / 今日计划 / 公告 / 个人中心 / 客观题练习 | ✅ |
| S6 | 联调验收、OpenAPI、README 本地启动说明（无 Docker） | ✅ |

工程入口（无 Docker）：

```bash
# API
python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000 --app-dir apps/api
# 学员端
npm run dev --prefix apps/web
# 管理端
npm run dev --prefix apps/admin
```

### Phase 1 — 深化（已完成）

| 步骤 | 范围 | 状态 |
|------|------|------|
| P1-A | 模型 Provider / Prompt 版本 / 用量日志（后台 AI 配置） | ✅ |
| P1-B | AI 学伴 SSE 流式对话 + 知识库关键词 RAG + 本地降级 | ✅ |
| P1-C | 主观题 LLM 初评 + 教师复核台 + **抽样质检** | ✅ |
| P1-D | 学情报表（含 **CSV 导出**）/ 错题本 / 消息中心 | ✅ |
| P1-E | 电子书 + 几何挂课 API | ✅ |
| P1-F | 知识库切片向量检索 + 检索测试 | ✅ |
| P1-G | 每日单词（含 **学习偏好设置**）/ 每日美文 | ✅ |
| P1-H | 英语语音评分 + 反馈工单 + 试卷/PPT 模板库 | ✅ |
| P1-I | **教师独立简化工作台**（`/hub`） | ✅ |

工程入口：学员端 `/ai` `/words` `/reading`；管理端「知识库 / 模板库 / 反馈工单」；OpenAPI `/docs`。

### Phase 2 — 教学深化（已深化完成）

| 步骤 | 范围 | 状态 |
|------|------|------|
| P2-A | 薄弱点练习推荐 + 推送深链题单 + 作答回写错题本/掌握 | ✅ |
| P2-B | LLM 成本看板（日期筛选 / 可配单价 / CSV 导出） | ✅ |
| P2-C | 课页↔题目显式关联 + 学完推荐练习 | ✅ |
| P2-D | 开放 API Token + 范围勾选（课程/试卷/公告/实验室） | ✅ |
| P2-E | 样本回流（来源/未导出筛选 + JSON/JSONL） | ✅ |
| P2-F | 图片读题、可复现变式练习、化学微观课页 | ✅ |
| P2-G | 学习路径引擎（`/path`）+ 简易工作流看板（`/workflows`） | ✅ |

工程入口：学员端 `/recommend` `/path`、几何课页练习；管理端「学情报表推送 / AI 成本看板 / 开放 API / 样本回流 / 几何实验室 / 工作流看板」。

### Phase 3 — 平台化（进行中）

| 步骤 | 范围 | 状态 |
|------|------|------|
| P3-A | PPT 真实导出 `.pptx`（管理端下载） | ✅ |
| P3-B | 数据备份脚本 `tools/backup.sh` | ✅ |
| P3-C | 学员数据导出 / 账号注销（合规） | ✅ |
| P3-D | 小学数学计算专项（学习中心课程） | ✅ |
| P3-E | 工作流事件规则引擎（批改/反馈/积压催办 + 执行日志；看板保留） | ✅ |
| P3-F | LTI/LMS 对接（简易 Launch + 演示页；生产需补 OAuth/LTI 1.3） | ✅ |
| P3-G | 多租户计费与用量包（配额校验/扣减 + 管理端看板） | ✅ |
| P3-H | 真实 Embedding API（OpenAI 兼容）+ 本地哈希回退；向量元数据落库（pgvector 仍可选） | ✅ |
| P3-I | 可选：外部微调任务对接（样本→Chat JSONL→任务桩 + Webhook） | ✅ |

工程入口：管理端「PPT 生成」导出；学员端「个人中心 → 隐私与账号」；运维 `./tools/backup.sh`；学员端 `/courses/math-calc`；管理端「租户用量包」；LTI 演示 `/api/v1/lti/demo`；管理端「样本回流」创建微调任务。

---

## 11. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 常规 API P95 < 300ms；上传/解析异步；对话首 Token < 2s（视模型） |
| 可用性 | Compose 部署目标 99%（单机）；支持数据备份脚本 |
| 安全 | HTTPS、RBAC、审计、上传白名单、密钥不落库明文（KMS/环境变量） |
| 合规 | 日志保留策略可配；支持数据导出/删除（账号注销） |
| 可访问性 | 关键操作键盘可达；对比度达标 |
| 国际化 | 首期中文；文案 Key 化便于 i18n |
| 浏览器 | Chrome / Safari / Edge 近两个正式版 |

---

## 12. 埋点与指标

### 12.1 产品指标

- DAU / WAU、学习时长、完课率、练习提交率
- AI：会话数、Token、点赞/点踩比、知识库命中率
- 几何动图：课页生成数、打开次数、人均停留时长、挂课使用率
- 经营：付费转化、会员续费、ARPU
- 质量：批改复核修改率、生成内容采纳率

### 12.2 技术指标

- 任务队列堆积、解析失败率、LLM 错误率、P95 延迟

---

## 13. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 宣传期望 = 自建训练平台 | 排期爆炸 | PRD 明确降维为网关+回流；对外话术改为「AI 工程闭环」 |
| LLM 成本失控 | 毛利为负 | 配额、缓存常见问、小模型分流 |
| 主观题评分不稳 | 信任崩塌 | 强制复核、置信度阈值、抽样质检 |
| 版权与内容安全 | 合规风险 | 上传协议、审核钩子、引用溯源 |
| 设计同质化 | 难建立品牌 | 执行第 5 章视觉规范，禁止紫白套版 |
| 过早微服务 | 交付变慢 | 坚持模块化单体 |
| edulab 升级破坏模板 | 课页生成失败 | 固定 vendoring 版本 + 生成回归脚本（demos 自检） |
| 交互课页 CSP / iframe 限制 | 学员端打不开 | 同域静态托管或放宽 frame-ancestors；优先同域 |

---

## 14. 里程碑验收清单（MVP）

- [x] 本地三进程一键启动（`./start.sh`，无 Docker）
- [x] 管理端：课程/用户/教师/班级/题库/试卷/助手/知识库/公告/订单基础闭环
- [x] 学员端：首页（新视觉）、学课、客观题考试、AI 对话（含 RAG）、打卡与计划
- [x] PPT 生成可导出（`.pptx`）
- [x] 仪表盘真数据（含学习行为分布）
- [x] RBAC + 审计日志可查询
- [x] OpenAPI 文档可访问
- [x] 品牌视觉符合第 5 章，而非截图复刻
- [x] 几何动图：演示课页可浏览；可挂交互课页；学员端可统计停留
- [x] 小学数学计算专项：年级选题 / 每日练习 / 判分错题本
- [x] 合规基础：个人数据导出 + 账号注销；运维备份脚本

---

## 15. 附录

### 15.1 和截图模块的映射

| 截图模块 | PRD 模块 ID | 备注 |
|----------|-------------|------|
| 仪表盘 | Analytics | 美化配色与空状态 |
| 课程/班级/用户/教师 | Catalog / Classroom / Identity | P0 |
| 电子书 | Catalog.Ebook | P1 |
| 题库/试卷/模板 | Assessment | 模板 P1 |
| PPT 模板/生成助手 | AI.Generation | P0 生成，P1 模板库 |
| AI 助手/知识库 | AI.Agent / AI.KB | P0 |
| 工作流 | Ops.Workflow | P2（固定流水线先行） |
| 公告 | Ops.Announcement | P0 |
| 学员 AI 列表/课程/打卡/计划/公告/单词 | Portal.* | 单词 P1；布局按 5.4 重构 |
| （新增）几何动图实验室 | GeometryLab | 来自 edulab，非截图原有模块 |

### 15.2 建议仓库初始化目录

```
eduAI/
  PRD.md
  README.md
  docker-compose.yml
  vendor/
    edulab/                 # 开源几何/化学交互课页（Apache-2.0）
  tools/
    geometry-lab/           # 统一生成入口与依赖
  content/
    geometry-lab/           # 已生成演示 HTML + 画廊 index
  apps/
    web/
    admin/
    api/
    worker/
  packages/
    shared/
    ui/
  docs/
    architecture.md
    api.md
    geometry-lab.md
```

### 15.3 下一步行动（给研发）

1. ✅ P0–P2 核心能力已落地（本地三进程，无 Docker）。  
2. ✅ PPT `.pptx` 导出、备份脚本、个人数据导出/注销、小学数学计算专项。  
3. ✅ Embedding API + 工作流 + 用量包 + LTI + 外部微调任务桩已落地；⏳ 可选深化：pgvector 原生索引。  
4. 可选：几何实验室人均停留/挂课率看板深化；英语陪练正式 LLM Key 运维配置。

---

## 16. 决策记录（ADR 摘要）

| 决策 | 结论 | 原因 |
|------|------|------|
| 是否自建训练平台 | 否（P3 前） | 成本高、客户少、可用 API 替代 |
| 向量库 | pgvector | 运维最简，满足万级文档 |
| 前端框架 | Vue 3 + Vite | 与现有界面技术栈契合、交付快 |
| 后端语言 | Python FastAPI | AI 落地效率 |
| 架构 | 模块化单体 | 可行、可售、可演进 |
| 视觉 | 青松+琥珀，重做信息架构 | 美化且差异化，避免套版 |
| 几何动图 | Vendoring edulab + CLI/Worker 封装 | 复用成熟 sympy+Three.js/Canvas 方案，避免自研几何引擎 |
| 化学微观动图 | P1 扩展，不阻塞几何 MVP | 同源仓库能力，优先级低于数理几何 |

---

## 17. 几何动图教学（edulab 专项）

### 17.1 背景与目标

高中立体几何与解析几何是「空间想象 / 参数变化」重灾区。传统录播与静态图无法让学生拖动参数验证结论。开源项目 **edulab** 将题目经 sympy 精确求解后，注入数据驱动模板，产出**自包含交互 HTML**：

- **立体几何**：Three.js 3D + MathJax，分步高亮棱/面/法向量  
- **解析几何**：2D Canvas + KaTeX，滑块驱动直线∩曲线、向量读数、范围条  
- **化学反应**（扩展）：分子级断键/成键微观动画  

eduAI 将其作为一等教学内容类型接入，服务「可演示、可挂课、可统计」的理科数字化课堂。

### 17.2 仓库落位（已实现脚手架）

| 路径 | 说明 |
|------|------|
| `vendor/edulab/` | 上游源码 vendoring（去嵌套 git），见 `VENDOR.md` |
| `tools/geometry-lab/generate.py` | 统一 CLI：solid / analytic / chem / demos |
| `tools/geometry-lab/requirements.txt` | `sympy` |
| `content/geometry-lab/` | 演示产出与画廊 `index.html` |
| `docs/geometry-lab.md` | 集成与运维说明 |

### 17.3 产品流程

```
教师选题型/输入参数（或随机）
        │
        ▼
  创建 lab_generation_jobs（异步）
        │
        ▼
  Worker 调用 tools/geometry-lab/generate.py
        │
        ▼
  HTML → MinIO/静态目录 → lab_pages.url
        │
        ├─► 管理端预览 / 挂到 lesson（type=interactive_lab）
        └─► 学员端画廊或课时内打开 → 上报学习时长
```

### 17.4 功能需求

#### 管理端 · 几何动图实验室

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 题型目录 | P0 | 立体：cube/box/pyramid/random；解析：上游 REGISTRY 列表 |
| 一键生成与预览 | P0 | 同步 CLI 已可用；产品化走异步任务 + 进度态 |
| 挂课 | P0 | 选择课程章节，写入课时 `content_type=interactive_lab` |
| 元数据 | P0 | 标题、学科标签、知识点、难度、来源参数 |
| 批量演示包 | P0 | 对应 `generate.py demos` |
| 图片读题 | P1 | 上游支持视觉读图；平台侧接多模态模型后回填 spec |
| 化学课页 | P1 | edu-chem-reaction |
| 与题库关联 | P1 | lab_page ↔ question / knowledge_point |

#### 学员端

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 实验室画廊 | P0 | 分组浏览立体 / 解析（视觉对齐第 5 章，非紫白套版） |
| 课时内学习 | P0 | iframe 或「全屏打开」；移动端优先全屏新页 |
| 学习行为 | P0 | 打开、停留时长、完成分步（若课页 postMessage 可达） |
| 练习联动 | P1 | 学完推荐同知识点客观题 |

### 17.5 API（MVP 草案）

```
GET  /api/v1/geometry-lab/catalog          # 题型与参数 schema
POST /api/v1/geometry-lab/jobs             # { skill, key, params, seed? }
GET  /api/v1/geometry-lab/jobs/{id}        # 状态 / 结果 URL
GET  /api/v1/geometry-lab/pages            # 已生成课页列表
POST /api/v1/geometry-lab/pages/{id}/attach-lesson
GET  /api/v1/geometry-lab/pages/{id}/content  # 或直接 CDN/静态 URL
```

### 17.6 非功能与合规

- 生成超时：单任务默认 60s，失败可重试 2 次  
- 产出 HTML 需消毒策略评估（模板可信 + 禁止用户注入任意脚本）  
- 保留上游 `LICENSE` / `NOTICE`；对外文档致谢 edulab  
- 依赖：运行环境 Python3 + sympy；无需 GPU  

### 17.7 验收标准（本专项）

- [x] 源码进入 `vendor/edulab`，统一入口可 `demos` 生成  
- [x] `content/geometry-lab/index.html` 画廊可本地静态预览  
- [ ] 管理端可视化实验室与挂课（Phase 1）  
- [ ] 学员端课程内打开并统计时长（Phase 1）  
- [ ] CI：`generate.py demos` 作为回归烟测（Phase 1）  

### 17.8 本地快速体验

```bash
python3 -m pip install -r tools/geometry-lab/requirements.txt
python3 tools/geometry-lab/generate.py demos
npm run geometry:demos   # 同步到 apps/web/public
npm run dev              # 学员端含三门热门课
```

---

## 18. P0 三门热门课规格（已交付）

### 18.1 总览

| ID | 课程名 | 路由 | 核心体验 |
|----|--------|------|----------|
| classroom | AI 互动课堂 | `/classroom` | 四场景编排（见 §19） |
| geometry-lab | 几何动图实验室 | `/courses/geometry-lab` | 立体/解析交互课页 |
| english-coach | 英语陪练 | `/courses/english-coach` | 情景口语对话 |
| ai-coding | 青少年 AI 编程入门 | `/courses/ai-coding` | 6 课编程路径 + 运行器 |

首页 `/` 以品牌 eduAI 为英雄信号，展示热门课卡片（含 AI 课堂）。

### 18.2 几何动图实验室

- 列表筛选：全部 / 立体几何 / 解析几何  
- 学习页 iframe 嵌入 `public/geometry-lab/**`  
- 数据源：`src/data/courses.ts` → `geometryLessons`  
- 重新生成：`npm run geometry:demos`

### 18.3 英语陪练

- 情景：咖啡店点单、校园自我介绍、机场问路、模拟面试  
- 默认 **本地规则陪练**；可选 LLM（`VITE_LLM_*`）  
- **语音**：Web Speech 识别（en-US）+ `speechSynthesis` 朗读；可关自动朗读  
- **进度**：登录后进入情景 → `started`；用户有效对话 ≥ 3 轮 → `completed`  
- 验收：麦克风授权后可把识别文本写入输入框；回复可朗读；进度百分比上升

### 18.4 青少年 AI 编程入门

- 6 课：AI 是什么 → 变量 → 循环 → 画画 → 迷你助手 → 打卡项目  
- 每课：概念文案 + 起始代码 + 提示 + 通关检查点 + `console` 练习场  
- **进度**：打开课 → `started`；运行成功 → `completed`  
- 验收：重置/运行可用；课间导航正常；登录后目录显示「已完成」

### 18.5 账号与进度（v1.3）

| 接口 | 说明 |
|------|------|
| `POST /api/v1/auth/register` | 注册并返回 JWT |
| `POST /api/v1/auth/login` | 登录 |
| `GET /api/v1/auth/me` | 当前用户 |
| `GET /api/v1/progress/me` | 三课汇总 + 明细 |
| `POST /api/v1/progress/upsert` | 上报 started/completed |

- 存储：SQLite `apps/api/data/eduai.db`  
- 前端：Vite 代理 `/api` → `:8000`；Token 存 `localStorage`  
- 几何进度：打开课页 `started`；停留 ≥ 20s 或点「标记学完」→ `completed`

### 18.6 视觉约束（本冲刺）

- 青松 `#0F6B5C` + 琥珀 `#E8A317`，标题用 Noto Serif SC  
- 禁止紫白套版；课程卡为可交互单元，允许使用  
- 首页含品牌英雄区 + 几何/口语/代码三处动效锚点

---

## 19. AI 互动课堂（OpenMAIC 启发）

参考：[OpenMAIC](https://open.maic.chat/home) / [清华 THU-MAIC 开源说明](https://github.com/THU-MAIC/OpenMAIC) ——「四种场景，一堂好课；AI 统筹编排」。

### 19.1 产品主张

| 场景 | 目标 | eduAI 实现 |
|------|------|------------|
| 幻灯讲解 | 听懂 | 逐条揭示 + AI 旁白 |
| 测验检验 | 检验 | 客观题即时反馈 |
| 模拟动手 | 动手 | 几何动图 iframe / 交互工作台 |
| PBL 创造 | 创造 | 角色 + 里程碑 + 交付草稿 |

多智能体：教师 / 同伴 / 导师人设旁路提示（非完整 LangGraph，可演进）。

### 19.2 路由与编排

- 大厅 `/classroom`：一句话成课 + 精选课  
- 播放器 `/classroom/:id`：侧栏大纲 + 场景舞台  
- 编排：`orchestrateClassroom(topic)` —— 有 LLM Key 走云端 JSON；否则本地四场景模板  
- 精选课 `line-plane-angle`：线面角 → 正方体几何模拟 → 讲解卡 PBL  

### 19.3 验收

- [x] 大厅展示四场景主张  
- [x] 可进入精选课走完幻灯→测验→模拟→PBL  
- [x] 一句话成课可生成并开课  
- [x] 登录后进度写入 `classroom` 课程汇总  
- [ ] Phase 1：白板激光笔、完整导出 pptx、多 Agent 实时对话  

---

**文档结束。** 变更需更新版本号并在评审会同步范围影响。
